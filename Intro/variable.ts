
let greetings: string = "Hello! Charrel Jame Eramis";
let age: number = 6; // float number still, number data type
let objs: object = {
    'name': 'charrel'
}
let isLogged: boolean = false;
let userId: number = 10.10;

let hero: boolean;

function getHero() {
    try {
        return true;
    } catch (error) {
        return false;
    }
}

// let heroes = ["spiderman", "batman", "superman"]
// heroes.map((hero) => { 
//     return `this is your ${hero}`
// })

hero = getHero();

userId.toFixed();
isLogged = true;

console.log(userId);
console.log(isLogged);
console.log(objs);

